---
apiVersion: v1
kind: Service
metadata:
  name: falcon-policy-audit
  namespace: endpoint-readiness-audit
  labels:
    app: falcon-policy-audit
    component: daemon
spec:
  type: ClusterIP
  ports:
    - name: health
      port: 8088
      targetPort: 8088
      protocol: TCP
  selector:
    app: falcon-policy-audit
    component: daemon
